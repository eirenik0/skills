Template for new skills. Copy this folder to `skills/<skill-name>/` and update `SKILL.md` so the `name` matches the directory.

If the skill uses MCP servers, keep `config/mcporter.json` and follow `mcp/README.md` for build steps.
